#pragma once
#include "raylib.h"

#pragma once
#include "raylib.h"

namespace ArcademiaKeybinds {
  enum ArcademiaKeybinds {
    P1_START = KEY_ENTER,
    P1_EXIT  = KEY_ESCAPE,

    P1_JOYSTICK_UP    = KEY_UP,
    P1_JOYSTICK_DOWN  = KEY_DOWN,
    P1_JOYSTICK_LEFT  = KEY_LEFT,
    P1_JOYSTICK_RIGHT = KEY_RIGHT,

    P1_A = KEY_Z,
    P1_B = KEY_X,
    P1_C = KEY_C,
    P1_D = KEY_V,
    P1_E = KEY_B,
    P1_F = KEY_N,

    P2_START = KEY_BACKSPACE,
    P2_EXIT  = KEY_Q,

    P2_JOYSTICK_UP    = KEY_W,
    P2_JOYSTICK_DOWN  = KEY_S,
    P2_JOYSTICK_LEFT  = KEY_A,
    P2_JOYSTICK_RIGHT = KEY_D,

    P2_A = KEY_F,
    P2_B = KEY_G,
    P2_C = KEY_H,
    P2_D = KEY_J,
    P2_E = KEY_K,
    P2_F = KEY_L,
  };
}